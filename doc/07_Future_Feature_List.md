## Future Feature List (Product Growth)

> Черновик идей для развития продукта.  
> Не является обязательным бэклогом — это «куда можем расти дальше».

---

### 1. Универсальная модель пользователей и групп

- **Отказ от жёсткой схемы «родитель–ребёнок»**  
  Любой пользователь может создавать свои карточки и учить их сам.  
  Роль «родителя» превращается в просто одного из пользователей, который может назначать наборы другим (детям, ученикам и т.д.).

- **Группы пользователей**  
  - Типы групп: класс, семья, друзья, учебная группа и т.п.  
  - Пользователь может состоять в нескольких группах.  
  - Внутри группы могут быть общие наборы карточек, доступные всем участникам.

> **NB:** Эту модель нужно дополнительно «прожарить» архитектурно: роли, права, владение контентом, доступ к статистике и т.п.

---

### 2. Шаринг и переиспользование наборов карточек

- **Общий каталог наборов**  
  - Любой пользователь видит все публичные наборы карточек.  
  - Возможность «подписаться» на набор и добавить его себе.

- **Назначение наборов другим пользователям**  
  - Из общего каталога или из своих наборов можно «закинуть» набор ребёнку / ученику / другу.  
  - Поддержка назначения набора сразу нескольким детям / участникам группы.

---

### 3. Персональное назначение наборов детям / ученикам

- **Учебный план на пользователя**  
  - Для каждого ребёнка выбираются конкретные наборы, которые он сейчас изучает.  
  - Родитель и сам ребёнок (если разрешено) могут добавлять/убирать наборы из его плана.  
  - Отдельный экран «что учит этот ребёнок сейчас».

---

### 4. Улучшенный режим повторения

- **Режимы практики**
  - **Flashcards**: классическое переворачивание карточки (слово → перевод).  
  - **Listening**: проигрываем только аудио — пользователь вспоминает слово/перевод.  
  - **Writing**: пользователь печатает перевод/слово.  
  - **Speaking**: пользователь произносит слово вслух (в перспективе — с распознаванием речи).

- **Фильтры для сессий повторения**
  - Только новые карточки.  
  - Только сложные карточки (с высоким количеством ошибок).  
  - Все карточки выбранного набора / пользователя.  
  - Карточки, не повторявшиеся X дней.

- **График повторений**
  - Отображение, когда и какие карточки были повторены.  
  - Визуализация прогресса по дням/неделям.

---

### 5. Progress Tracking и награды

- **Дневная статистика**
  - Сколько карточек пользователь/ребёнок повторил за день.  
  - Серии дней (streak), визуализация по календарю.

- **Недельные цели и награды**
  - Цель: заполнить неделю по X карточек в день.  
  - При выполнении цели — награда (бейджи, ачивки, внутренняя валюта, стикеры и т.п.).

---

### 6. Групповая динамика и соревнование

- **Лидерборды по группе**
  - Кто сколько карточек выучил.  
  - Сколько дней подряд занимается каждый участник.  
  - Сравнение прогресса внутри класса, семьи, дружественной группы.

- **Мотивационные механики**
  - Совместные челленджи (например: «вся группа учит по 20 карточек в день неделю подряд»).  
  - Публичные ачивки внутри группы.

---

### 7. Интервальное повторение (Spaced Repetition)

- **Базовый алгоритм**
  - День 1: учу слово.  
  - День 2: повтор; если ответ правильный → следующая проверка через 4 дня.  
  - День 6: повтор; если правильный → через 7 дней.  
  - День 13: повтор; если правильный → через 14 дней.  
  - Далее — увеличение интервалов.

- **Упрощённая версия для MVP 2.0**
  - Фиксированные интервалы: 1 день, 3 дня, 7 дней, 14 дней, 30 дней.  
  - При ошибке интервал сбрасывается на начальный.

---

### 8. Улучшенный AUDIO (TTS)

- **Более «человечный» TTS для RU/EN**
  - Поиск и интеграция бесплатной или условно-бесплатной модели TTS с естественным голосом (русский и английский).  
  - Поддержка озвучивания отдельных слов и примеров использования.  
  - В перспективе — выбор голоса/акцента.

---

### 9. Генерация короткого текста из выученных слов

- **Идея**  
  Сервис генерирует короткий связный текст (2–3 предложения или короткий рассказ), используя слова из карточек, которые пользователь выучил, или из всего доступного словаря.

- **Методика по объёму**
  - Мало слов → 2–3 предложения.  
  - Больше слов → короткий рассказ.  
  - Неизвестные слова добавляются по минимуму — только связки для смысла (артикли, предлоги, союзы, базовые глаголы и т.п.).

- **Уровень сложности (настраивается)**
  - Влияет на количество «связующих» неизвестных слов и на грамматику.  
  - **Базовый уровень:** настоящее время (Present Simple и т.п.), минимум связок, короткие предложения.  
  - Более высокие уровни: прошлое/будущее время, более сложные конструкции, больше связок.

- **Источник слов**
  - Только выученные карточки (по прогрессу пользователя).  
  - Или весь словарь набора / нескольких наборов (опция «из всего словаря»).

---

### 10. Статус изучения набора

- **По каждому набору показывать:** сколько карточек выучил, сколько ещё не выучено (новые / в процессе).
- Отдельная сводка по набору: выучено / всего, процент.
- Направление изучения (RU→EN / EN→RU) не учитывать — только факт: **знаю / не знаю** слово (одна метрика на карточку).
- Показывать только ребёнку (ученику), чтобы он видел прогресс по набору «сразу в цифрах».

**Эталон отображения (уже есть)**  
Раздел **«Проверка» → Выбери тему для проверки** (`/student/test/by-deck`): на каждой карточке набора блок с **Изучено** (📝), **Всего** (📚), подпись **«Прогресс изучения X%»** и прогресс-бар. Оформление: два блока в ряд (bg-blue-50 / bg-gray-50), под ними строка с процентами и полоска. Этот же формат использовать везде.

**План реализации**

| # | Что | Где |
|---|-----|-----|
| 1 | **Общая утилита прогресса по набору** | `src/lib/utils/deck-progress.ts` (новый файл) |
| 2 | **Общий UI-блок «Изучено / Всего / Прогресс изучения»** | `src/components/student/DeckProgressStats.tsx` (новый компонент) |
| 3 | **Список наборов** | `src/app/student/decks/page.tsx` |
| 4 | **Обложка набора (дашборд)** | `src/app/student/page.tsx` |
| 5 | **Шапка внутри набора (где выбор направления)** | `src/app/student/decks/[id]/page.tsx` |
| 6 | **Проверка — перейти на утилиту и компонент** | `src/app/student/test/by-deck/page.tsx` |

**Детали по пунктам**

1. **`src/lib/utils/deck-progress.ts`**  
   Функции: по `userId` и `deckId` (или массиву `deckIds`) возвращать для набора `total`, `mastered`, `percent`.  
   - **Выучено (mastered):** карточки, у которых есть хотя бы одна запись в `card_progress` со статусом `mastered` (направление не учитываем).  
   Для списка наборов — `getDecksProgress(userId, deckIds)` с батч-запросом (карточки по deck_ids, `card_progress` пользователя по этим card_id), агрегация по наборам (без N+1).

2. **`src/components/student/DeckProgressStats.tsx`**  
   Компонент с пропсами `studiedCount: number`, `totalCount: number`. Рендерит тот же блок, что в «Проверка»: два блока (Изучено 📝, Всего 📚), строка «Прогресс изучения X%» и прогресс-бар. Опционально проп `variant?: 'default' | 'onDark'` для отображения на градиентном фоне (обложка набора, карточки в списке/дашборде — полупрозрачный фон или контрастный текст).

3. **`src/app/student/decks/page.tsx`**  
   Загружать прогресс через утилиту для всех наборов. В карточку каждого набора добавить `<DeckProgressStats studiedCount={…} totalCount={…} />` (тот же вид, что в Проверке). При необходимости перейти на белые карточки (`bg-white rounded-2xl …`) как в разделе Проверка для единообразия, либо использовать `variant="onDark"` на текущих градиентных карточках.

4. **`src/app/student/page.tsx`**  
   Для последних 5 наборов запрашивать прогресс (утилита). На обложку каждого набора добавить блок Изучено / Всего / Прогресс изучения через `<DeckProgressStats />` (вариант для тёмного фона, если карточки остаются градиентными).

5. **`src/app/student/decks/[id]/page.tsx`**  
   Запрашивать прогресс по этому `deckId` (утилита). В шапку страницы (градиентный блок с названием набора и выбором направления) добавить блок Изучено / Всего / Прогресс изучения — через `<DeckProgressStats />` с вариантом для тёмного фона (в шапке фон градиентный).

6. **`src/app/student/test/by-deck/page.tsx`**  
   Заменить текущий подсчёт прогресса на вызов утилиты `getDecksProgress`. Заменить текущую разметку блока статистики и прогресс-бара на использование `<DeckProgressStats studiedCount={…} totalCount={…} />`, чтобы вид был один и тот же и правки в будущем в одном месте.

**Итог:** один формат отображения (Изучено / Всего / Прогресс изучения) в четырёх местах: список наборов, обложки на дашборде, шапка внутри набора, выбор набора для проверки. Данные — из общей утилиты, UI — из общего компонента.

**Не трогать:** родительские экраны и API.

---

### 11. Список изученных слов («Знаю»)

- **Где-то в интерфейсе показывать изученные слова** — те карточки, которые пользователь отметил как «Знаю» (в режиме изучения/повторения).
- Источник данных: карточки, по которым в `card_progress` есть запись со статусом `mastered` (или текущий эквивалент «отметил Знаю»). Сейчас это поле в БД есть, но не заполняется при нажатии «Знаю» — нужно завести единую логику «изучено = Знаю».
- Отображение: отдельный экран или блок «Мои изученные слова» с возможностью фильтра по набору, поиска, при желании — экспорта.

---

### 12. Список выученных слов (успех в тесте)

- **Отдельным списком показывать выученные слова** — те, на которые пользователь **правильно ответил в тесте** (в проверке). Это другая метрика, чем «изученные»: изученное = отметил «Знаю» при показе карточки; выученное = правильно ответил в тесте.
- **Нужно добавить новое значение/параметр для слова (карточки):** сейчас логики «правильно ответил в тесте по этой карточке» в прогрессе карточки нет. Варианты: новое поле в `card_progress` (например, `passed_in_test_at` или флаг «успешно пройдена в тесте»), либо выводить список из уже существующих `test_results` / сессий тестов (по `card_id` + `is_correct`). В любом случае — явно зафиксировать в модели «это слово выучено (успех в тесте)» и показывать отдельный список «Выученные слова».

---


