# 🔍 Диагностика проблем с авторизацией

## Проблема: Профиль не загружается (таймаут 5 секунд)

Если вы видите в консоли:
```
⏱️ AuthContext: Profile load timeout, forcing loading=false
```

Это означает, что запрос к таблице `profiles` не возвращает результат в течение 5 секунд.

---

## Шаг 1: Проверить логи в консоли браузера

После добавления детального логирования вы должны увидеть:

```
📋 AuthContext: Loading profile for user: <USER_ID>
📋 AuthContext: Starting Supabase query...
📋 AuthContext: Query completed in XXXms
📋 AuthContext: Query result: { data: ..., error: ..., hasData: ..., hasError: ... }
```

### Что искать:
- **Если "Query completed" НЕ появляется** → запрос зависает (проблема с сетью или Supabase)
- **Если "hasError: true"** → смотрите полный объект ошибки ниже
- **Если "hasData: false" и "hasError: false"** → профиль не существует в БД

---

## Шаг 2: Использовать страницу диагностики

Откройте в браузере:
```
http://localhost:3000/debug-auth
```

Эта страница покажет:
- ✅ Текущую сессию пользователя
- ✅ Результат запроса профиля
- ✅ Время выполнения запроса
- ✅ Детальные ошибки

---

## Шаг 3: Проверить RLS политики в Supabase

1. Откройте **Supabase Dashboard** → SQL Editor
2. Запустите скрипт `supabase/debug_rls.sql`

### Что проверить:

#### 1. RLS включён?
```sql
SELECT tablename, rowsecurity as rls_enabled
FROM pg_tables
WHERE schemaname = 'public' AND tablename = 'profiles';
```
**Ожидаемый результат:** `rls_enabled = true`

#### 2. Политики существуют?
```sql
SELECT policyname, cmd, roles
FROM pg_policies
WHERE schemaname = 'public' AND tablename = 'profiles';
```
**Должно быть 4 политики:**
- `Enable insert for authenticated users` (INSERT)
- `Enable read for authenticated users` (SELECT)
- `Enable update for users based on id` (UPDATE)
- `Enable delete for users based on id` (DELETE)

#### 3. Профиль существует?
```sql
SELECT * FROM profiles WHERE id = '<USER_ID>';
```
Замените `<USER_ID>` на ID из логов (например, `cac4a501-f086-4b6c-b50b-a189ccd1560b`)

**Если профиль НЕ найден** → нужно создать профиль:
```sql
INSERT INTO profiles (id, display_name, role, family_id)
VALUES (
  '<USER_ID>',
  'Имя пользователя',
  'admin', -- или 'student'
  '<FAMILY_ID>'
);
```

---

## Шаг 4: Переприменить RLS политики

Если политики отсутствуют или неправильные:

1. Откройте **Supabase Dashboard** → SQL Editor
2. Запустите скрипт `supabase/reapply_rls.sql`
3. Проверьте, что все 4 политики созданы

---

## Шаг 5: Проверить связь auth.users ↔ profiles

Запустите в Supabase SQL Editor:

```sql
-- Найти пользователей без профилей
SELECT 
  u.id,
  u.email,
  u.created_at
FROM auth.users u
LEFT JOIN profiles p ON u.id = p.id
WHERE p.id IS NULL;
```

Если есть пользователи без профилей → создайте профили для них.

---

## Шаг 6: Тест доступа от имени пользователя

В Supabase SQL Editor можно протестировать запрос от имени конкретного пользователя:

```sql
-- 1. Получить JWT токен пользователя из браузера (Application → Storage → Local Storage)
-- Ключ: sb-<project-id>-auth-token

-- 2. Установить роль
SET ROLE authenticated;
SET request.jwt.claim.sub = '<USER_ID>';

-- 3. Попробовать запрос
SELECT * FROM profiles WHERE id = '<USER_ID>';

-- 4. Вернуть роль
RESET ROLE;
```

---

## Частые причины проблемы:

### 1. ❌ Профиль не существует в БД
**Решение:** Создайте профиль вручную через SQL или через onboarding

### 2. ❌ RLS политики не применены
**Решение:** Запустите `supabase/reapply_rls.sql`

### 3. ❌ Неправильный USER_ID
**Решение:** Проверьте `auth.users.id` и `profiles.id` - они должны совпадать

### 4. ❌ Проблема с сетью / Supabase недоступен
**Решение:** Проверьте интернет соединение, статус Supabase (status.supabase.com)

### 5. ❌ Кэш браузера
**Решение:** Очистите кэш, localStorage, или откройте в режиме инкогнито

---

## Логи для анализа

При обращении к поддержке предоставьте:

1. **Логи из консоли браузера** (полностью)
2. **Результат `/debug-auth` страницы** (скриншот или JSON)
3. **Результат `supabase/debug_rls.sql`**
4. **User ID из логов**

---

## Быстрое решение (если срочно)

Если нужно быстро разблокировать вход:

1. Найдите `USER_ID` в логах консоли
2. Откройте Supabase SQL Editor
3. Запустите:

```sql
-- Проверить есть ли профиль
SELECT * FROM profiles WHERE id = '<USER_ID>';

-- Если НЕТ - создать
INSERT INTO profiles (id, display_name, role, family_id)
VALUES (
  '<USER_ID>',
  'Test User',
  'admin',
  (SELECT id FROM families LIMIT 1) -- взять любую семью
);

-- Если ЕСТЬ но не загружается - проверить RLS
SELECT * FROM pg_policies WHERE tablename = 'profiles';
```

4. Обновите страницу логина

---

## Контакты

Если проблема не решена после всех шагов:
- Проверьте GitHub Issues: https://github.com/bcherednikov/kotocard/issues
- Создайте новый Issue с логами
