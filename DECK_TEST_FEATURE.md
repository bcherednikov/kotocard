# 📚 Тестирование по конкретному набору ("По теме")

## Дата: 2026-02-05

## Новая функциональность:

Добавлена возможность тестирования по конкретному набору карточек, в дополнение к существующему "Случайному миксу".

---

## Структура:

### 1. Страница выбора режима теста
**Путь:** `/student/test`

**Изменения:**
- Обновлен дизайн - теперь 2 карточки в ряд
- Добавлена новая карточка "📚 По теме"

**Режимы:**
```
┌─────────────────┬─────────────────┐
│ 🎲 Случайный    │ 📚 По теме      │
│    микс         │                 │
│                 │                 │
│ 10 случайных    │ Выбери набор и  │
│ вопросов из     │ проверь знания  │
│ всех слов       │ по теме         │
└─────────────────┴─────────────────┘
```

---

### 2. Страница выбора набора
**Путь:** `/student/test/by-deck`

**Функционал:**
- ✅ Показывает все наборы, из которых ученик изучил карточки
- ✅ Для каждого набора отображает:
  - Название и описание
  - Количество изученных карточек
  - Общее количество карточек
  - Прогресс-бар изучения
  - Тэги набора
- ✅ Фильтрация: показываются только наборы с изученными карточками (studiedCardsCount > 0)
- ✅ Если нет доступных наборов - предложение перейти к обучению

**Дизайн:**
```
┌─────────────────────────────────────┐
│ ← Назад к выбору режима             │
│                                     │
│      📚 Выбери тему для проверки    │
│                                     │
├─────────────────┬─────────────────┤
│ Набор 1         │ Набор 2         │
│                 │                 │
│ Описание...     │ Описание...     │
│                 │                 │
│ 📝 Изучено: 15  │ 📝 Изучено: 20  │
│ 📚 Всего: 50    │ 📚 Всего: 30    │
│                 │                 │
│ Прогресс: 30%   │ Прогресс: 67%   │
│ [════      ]    │ [========  ]    │
└─────────────────┴─────────────────┘
```

---

### 3. Страница теста по набору
**Путь:** `/student/test/by-deck/[deckId]`

**Функционал:**
- ✅ Загружает карточки только из выбранного набора
- ✅ Фильтрует только изученные карточки
- ✅ Генерирует 10 случайных вопросов (или меньше, если карточек мало)
- ✅ Использует ту же логику тестирования, что и "Случайный микс":
  - Разные типы вопросов: текст, аудио
  - Разные режимы ответа: выбор, диктант, аудио
  - Всегда проверяет перевод (противоположный язык)
  - Динамические инструкции
  - Мгновенная обратная связь
- ✅ Сохраняет результаты в `test_sessions` и `test_results`
- ✅ Показывает название набора в шапке теста
- ✅ После завершения редиректит на страницу результатов

**Дизайн:**
```
┌─────────────────────────────────────┐
│      [📚 Название набора]           │
│                                     │
│ Вопрос 3 из 10              30%     │
│ [======                    ]        │
│                                     │
│ ┌─────────────────────────────┐    │
│ │ 📝 Напиши перевод на русском│    │
│ ├─────────────────────────────┤    │
│ │                             │    │
│ │        Apple                │    │
│ │                             │    │
│ ├─────────────────────────────┤    │
│ │ [Введи ответ...         ]   │    │
│ │ [Проверить]                 │    │
│ └─────────────────────────────┘    │
│                                     │
│ ← Выбрать другой набор              │
└─────────────────────────────────────┘
```

---

## Технические детали:

### Файлы созданы:
1. **`src/app/student/test/by-deck/page.tsx`**
   - Страница выбора набора
   - Загружает наборы с подсчетом изученных карточек
   - Фильтрует доступные наборы

2. **`src/app/student/test/by-deck/[deckId]/page.tsx`**
   - Страница теста по набору
   - Переиспользует логику из `random-mix/page.tsx`
   - Добавлен фильтр по `deck_id`

### Файлы изменены:
1. **`src/app/student/test/page.tsx`**
   - Обновлен дизайн (2 колонки)
   - Добавлена карточка "По теме"
   - Улучшен UI (градиенты, теги)

---

## Логика работы:

### 1. Выбор набора:
```typescript
// Загрузить изученные карточки
const { data: progress } = await supabase
  .from('card_progress')
  .select('card_id')
  .eq('user_id', user.id);

// Загрузить карточки из набора
const { data: cards } = await supabase
  .from('cards')
  .select('*')
  .eq('deck_id', deckId)  // ← Фильтр по набору!
  .in('id', studiedCardIds);

// Показать только наборы где есть изученные карточки
const availableDecks = decks.filter(d => d.studiedCardsCount > 0);
```

### 2. Генерация теста:
- Та же логика, что и в "Случайном миксе"
- Отличие: карточки фильтруются по `deck_id`
- Минимум 4 карточки для теста

### 3. Сохранение результатов:
- Используется та же таблица `test_sessions`
- Поле `mode` можно расширить для разных типов тестов (но сейчас не критично)
- Результаты отображаются на странице `/student/test/random-mix/complete`

---

## Требования:

### Минимальные требования для теста:
- ✅ Минимум 4 изученные карточки в наборе
- ✅ Пользователь должен был хотя бы раз изучать карточки из набора

### Если требования не выполнены:
- Набор не показывается в списке доступных
- При попытке зайти напрямую - алерт и редирект

---

## Навигация:

```
/student/test
  ├── Случайный микс → /student/test/random-mix
  └── По теме → /student/test/by-deck
                  └── Выбор набора
                      └── /student/test/by-deck/[deckId]
                          └── Тест по набору
                              └── /student/test/random-mix/complete
                                  └── Результаты
```

---

## Преимущества:

### Для ученика:
- 🎯 Целевое повторение конкретной темы
- 📊 Видит прогресс по каждому набору
- 🔄 Может повторять тест по сложной теме
- 📖 Выбирает что учить

### Для родителя:
- 📚 Ребенок может сосредоточиться на конкретной теме
- 🎓 Проверка знаний по пройденному материалу
- 📈 Видно какие темы даются легче/сложнее

---

## Будущие улучшения (опционально):

1. **Статистика по набору:**
   - Средний балл по набору
   - Количество попыток
   - Сложные слова из набора

2. **Фильтры на странице выбора:**
   - По тэгам
   - По проценту изучения
   - По дате создания

3. **Разные режимы теста:**
   - Быстрый (5 вопросов)
   - Полный (все изученные карточки)
   - Экзамен (со временем)

4. **Рекомендации:**
   - "Рекомендуем повторить" (низкий балл)
   - "Давно не проходил тест по этой теме"

---

## Тестирование:

### Тест 1: Выбор набора
1. Войти как ученик
2. Перейти в "🎯 Проверка"
3. Выбрать "📚 По теме"
4. Увидеть список наборов с изученными карточками

### Тест 2: Прохождение теста
1. Выбрать набор
2. Пройти тест (10 вопросов)
3. Убедиться, что все вопросы из выбранного набора
4. Проверить сохранение результатов

### Тест 3: Граничные случаи
1. Набор с менее чем 4 карточками → алерт
2. Набор без изученных карточек → не показывается
3. Попытка зайти по прямой ссылке → редирект

---

## Результат:

### До:
- ❌ Только "Случайный микс" из всех карточек
- ❌ Нельзя сфокусироваться на конкретной теме

### После:
- ✅ Два режима: "Случайный микс" и "По теме"
- ✅ Выбор конкретного набора для теста
- ✅ Статистика по каждому набору
- ✅ Целевая проверка знаний по теме
