# 📱 Адаптивное бургер-меню для мобильных устройств

## Дата: 2026-02-05

## Новая функциональность:

Добавлено адаптивное меню, которое автоматически сворачивается в "бургер" (☰) на мобильных устройствах.

---

## Изменения:

### Header Component (`src/components/layout/Header.tsx`)

#### 1. **Состояние меню**
```typescript
const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);
```

#### 2. **Бургер-кнопка**
- Видна только на мобильных устройствах (`md:hidden`)
- Анимированная трансформация в "X" при открытии
- Плавная анимация линий (rotate, translate)

```tsx
<button className="md:hidden">
  <div className="w-6 h-5">
    <span className={rotate-45 translate-y-2}></span>  // Верхняя линия
    <span className={opacity-0}></span>                // Средняя линия
    <span className={-rotate-45 -translate-y-2}></span> // Нижняя линия
  </div>
</button>
```

#### 3. **Десктопное меню**
- Скрыто на мобильных (`hidden md:flex`)
- Видимо на планшетах и десктопах (≥768px)
- Горизонтальное расположение

#### 4. **Мобильное меню**
- Скрыто на десктопах (`md:hidden`)
- Выезжает сверху вниз с анимацией
- Вертикальное расположение ссылок
- Закрывается при клике на ссылку

```tsx
<div className={`
  md:hidden 
  overflow-hidden 
  transition-all 
  duration-300 
  ${isMobileMenuOpen ? 'max-h-screen opacity-100' : 'max-h-0 opacity-0'}
`}>
  <nav className="py-4 space-y-2">
    // Ссылки меню
  </nav>
</div>
```

---

## Функциональность:

### 1. **Автоматическое закрытие**

**При изменении размера окна:**
```typescript
useEffect(() => {
  function handleResize() {
    if (window.innerWidth >= 768) {
      setIsMobileMenuOpen(false);
    }
  }
  window.addEventListener('resize', handleResize);
}, []);
```

**При нажатии Escape:**
```typescript
useEffect(() => {
  function handleEscape(e: KeyboardEvent) {
    if (e.key === 'Escape') {
      setIsMobileMenuOpen(false);
    }
  }
  document.addEventListener('keydown', handleEscape);
}, [isMobileMenuOpen]);
```

**При клике на ссылку:**
```typescript
function closeMobileMenu() {
  setIsMobileMenuOpen(false);
}
// Каждая ссылка: onClick={closeMobileMenu}
```

### 2. **Overlay (затемненный фон)**
- Появляется при открытии меню
- Полупрозрачный черный фон (`bg-black/50`)
- Закрывает меню при клике вне его
- Только на мобильных устройствах

```tsx
{isMobileMenuOpen && (
  <div 
    className="fixed inset-0 bg-black/50 z-40 md:hidden"
    onClick={closeMobileMenu}
  />
)}
```

### 3. **Блокировка скролла**
При открытом меню страница не скроллится:
```typescript
if (isMobileMenuOpen) {
  document.body.style.overflow = 'hidden';
} else {
  document.body.style.overflow = 'unset';
}
```

### 4. **Sticky Header**
Header закреплен вверху страницы:
```tsx
<header className="sticky top-0 z-50">
```

---

## Breakpoints (Tailwind):

- **Mobile:** < 768px (меню сворачивается)
- **Desktop:** ≥ 768px (обычное горизонтальное меню)

Префикс `md:` = medium breakpoint (768px)

---

## Анимации:

### Бургер-кнопка → X:
```
Закрыто:          Открыто:
  ———               ╲
  ———                ╳
  ———               ╱
```

**CSS классы:**
- `transition-all duration-300` - плавная анимация
- `rotate-45` - поворот на 45°
- `translate-y-2` - сдвиг вверх
- `opacity-0` - скрытие средней линии

### Выезд меню:
```
Закрыто:          Открыто:
┌─────────┐       ┌─────────┐
│  ☰      │       │  ✕      │
└─────────┘       ├─────────┤
                  │ 🏠 Главная │
                  │ 📚 Наборы  │
                  │ 🎯 Проверка│
                  └─────────┘
```

**CSS классы:**
- `max-h-0` → `max-h-screen` - высота от 0 до полного экрана
- `opacity-0` → `opacity-100` - прозрачность от 0 до 100%
- `overflow-hidden` - скрывает переполнение во время анимации

---

## Структура меню:

### Для студента (мобильное):
```
┌─────────────────────┐
│ 📚 KotoCard    ☰    │ ← Header
├─────────────────────┤
│ 🏠 Главная          │
│ 📚 Наборы           │
│ 🎯 Проверка         │
│ 📊 Результаты       │
├─────────────────────┤
│ 👤 Имя пользователя │
│ 🚪 Выход            │
└─────────────────────┘
```

### Для родителя (мобильное):
```
┌─────────────────────┐
│ 📚 KotoCard    ☰    │
├─────────────────────┤
│ 📚 Наборы           │
│ 👶 Дети             │
├─────────────────────┤
│ 👤 Имя пользователя │
│ 🚪 Выход            │
└─────────────────────┘
```

### Для неавторизованных (мобильное):
```
┌─────────────────────┐
│ 📚 KotoCard    ☰    │
├─────────────────────┤
│ 🏠 Главная          │
│ 🔑 Вход             │
│ ✨ Регистрация      │
└─────────────────────┘
```

---

## Стилизация:

### Мобильные ссылки:
- Блочные (`block`)
- Полная ширина (`w-full`)
- Отступы: `px-4 py-3`
- Hover эффект: `hover:bg-gray-100`
- Закругленные углы: `rounded-lg`
- Иконки для визуальной навигации

### Кнопка выхода:
- Красный текст: `text-red-600`
- Красный фон при hover: `hover:bg-red-50`
- Выделена визуально от других пунктов

### Имя пользователя:
- Серый текст: `text-gray-600`
- Граница сверху: `border-t`
- Визуально отделено от кнопок

---

## Z-index слои:

```
50 - Header (sticky)
40 - Overlay (backdrop)
10 - Мобильное меню (контент)
```

---

## Accessibility (Доступность):

1. **Keyboard navigation:**
   - ESC закрывает меню
   - Tab навигация работает

2. **ARIA labels:**
   - Кнопка: `aria-label="Toggle menu"`

3. **Focus management:**
   - Фокус сохраняется при переключении

4. **Screen readers:**
   - Все ссылки доступны для чтения

---

## Тестирование:

### Desktop (≥768px):
- ✅ Горизонтальное меню видимо
- ✅ Бургер-кнопка скрыта
- ✅ Overlay не появляется

### Mobile (<768px):
- ✅ Бургер-кнопка видима
- ✅ Меню сворачивается
- ✅ Клик на бургер открывает меню
- ✅ Overlay появляется
- ✅ Клик на overlay закрывает меню
- ✅ Клик на ссылку закрывает меню
- ✅ ESC закрывает меню
- ✅ Скролл блокируется при открытом меню

### Transitions:
- ✅ Плавная анимация открытия/закрытия
- ✅ Бургер трансформируется в X
- ✅ Overlay fade in/out

---

## Преимущества:

### UX:
- 📱 Оптимизировано для мобильных устройств
- 👆 Легко открывать/закрывать одним касанием
- 🎨 Красивые анимации
- 🚀 Быстрый доступ ко всем разделам
- 📐 Не занимает место на маленьких экранах

### DX (Developer Experience):
- 🔧 Использует Tailwind breakpoints
- 🎯 Простой state management
- 🧩 Переиспользуемый компонент
- 📝 Чистый и читаемый код

---

## Возможные улучшения (будущее):

1. **Анимация появления ссылок:**
   - Каскадное появление пунктов меню
   - Stagger animation

2. **Жесты:**
   - Swipe для закрытия меню
   - Pull-to-close

3. **Submenu:**
   - Вложенные меню для категорий
   - Dropdown на мобильных

4. **Search:**
   - Поиск в мобильном меню
   - Быстрый переход к разделам

---

## Результат:

### До:
- ❌ Меню не адаптивно
- ❌ На мобильных ссылки сжимаются
- ❌ Неудобная навигация на маленьких экранах

### После:
- ✅ Полностью адаптивное меню
- ✅ Бургер-меню на мобильных
- ✅ Плавные анимации
- ✅ Overlay и блокировка скролла
- ✅ Автоматическое закрытие (ESC, click, resize)
- ✅ Sticky header
- ✅ Красивый UX на всех устройствах
